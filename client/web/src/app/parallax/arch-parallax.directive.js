'use strict'
angular.module('archCarto')
  .directive('archParallax', [
    function() {
      return {
        restrict: 'A',
        link: function(scope, element, attributes) {
          debugger;
        }
      };
    }
  ]);
